version: "3.9"
services:
  tenebra:
    build: .
#    image: "quantumlytangled/krist"
    environment:
      - DB_NAME=tenebra
      - DB_USER=tenebra
      - DB_PASS=aoBagH93Q1Fk1IcA418V0hjl
      - PUBLIC_URL=tenebra.lil.gay
      - DB_HOST=127.0.0.1
      - REDIS_HOST=127.0.0.1
      - REDIS_PREFIX="tenebra:"
      - MINING_ENABLED=true
      - GEN_GENESIS=true
      - WEB_LISTEN=8142
      - NODE_ENV=production
    #ports:
    #  - "127.0.0.1:8142:8142"
    network_mode: host
    restart: unless-stopped